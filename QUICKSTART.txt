╔══════════════════════════════════════════════════════════════╗
║   STUDENT PERFORMANCE PREDICTION - QUICK START GUIDE        ║
╚══════════════════════════════════════════════════════════════╝

🌟 LIVE WEB APPLICATION - Get Started in 3 Steps!

┌─────────────────────────────────────────────────────────────┐
│ STEP 1: Install Dependencies                                │
└─────────────────────────────────────────────────────────────┘

Open your terminal/command prompt in this folder and run:

    pip install -r requirements.txt

This installs all required Python libraries.

┌─────────────────────────────────────────────────────────────┐
│ STEP 2: Launch the Web Application                          │
└─────────────────────────────────────────────────────────────┘

Choose one of these methods:

METHOD A - Direct Command:
    streamlit run app.py

METHOD B - Using Launcher (Linux/Mac):
    bash run_app.sh

METHOD C - Using Launcher (Windows):
    Double-click run_app.bat

┌─────────────────────────────────────────────────────────────┐
│ STEP 3: Use the Application                                 │
└─────────────────────────────────────────────────────────────┘

The app will open in your browser at: http://localhost:8501

1. Click "Train/Retrain Models" in the sidebar
2. Go to "Make Prediction" tab
3. Adjust the sliders for student details
4. Click "Predict Performance"
5. View results, recommendations, and visualizations!

═══════════════════════════════════════════════════════════════

📋 WEB APP FEATURES:

✓ Interactive Prediction Interface
  → Real-time predictions with sliders
  → Instant results with grade display
  → Beautiful gauge visualizations

✓ Model Performance Dashboard
  → Compare Linear Regression vs Decision Tree
  → View accuracy metrics
  → Analyze actual vs predicted plots

✓ Data Analytics
  → Explore the dataset
  → View distributions and correlations
  → Download data as CSV

✓ Prediction History
  → Track all your predictions
  → View trends over time
  → Export history to CSV

═══════════════════════════════════════════════════════════════

🎯 SAMPLE USE CASE:

1. Train models (sidebar button)
2. Enter student details:
   - Attendance: 85%
   - Study Hours: 6 hours/day
   - Previous Marks: 75
   - Assignment Scores: 80
3. Click "Predict Performance"
4. Get prediction: ~75-80 marks (Grade B)
5. Review personalized recommendations

═══════════════════════════════════════════════════════════════

💡 TIPS:

→ Adjust dataset size in sidebar (100-500 students)
→ Switch between models to compare results
→ Use "Both" option to see both predictions
→ Download prediction history for analysis
→ Export datasets for external use

═══════════════════════════════════════════════════════════════

🛠️ TROUBLESHOOTING:

Problem: Port already in use
Solution: streamlit run app.py --server.port 8502

Problem: Module not found
Solution: pip install -r requirements.txt --upgrade

Problem: App won't start
Solution: Make sure Python 3.8+ is installed
          python --version

═══════════════════════════════════════════════════════════════

📚 ADDITIONAL SCRIPTS:

Command Line Tools (if you prefer CLI):

1. Full ML Pipeline:
   python student_performance_predictor.py

2. Interactive Prediction:
   python predict_new_student.py

═══════════════════════════════════════════════════════════════

🎓 EDUCATIONAL VALUE:

This project demonstrates:
→ Machine Learning model training
→ Web application development
→ Interactive data visualization
→ Real-time predictions
→ Model comparison and evaluation

Perfect for presentations, demos, and learning!

═══════════════════════════════════════════════════════════════

Need help? Check README.md for detailed documentation.

Happy Predicting! 🚀
